// Exists determines if the {{ .Name }} exists in the database.
func ({{ shortname .Name }} *{{ .Name }}) Exists() bool {
	return {{ shortname .Name }}._exists
}

// Deleted provides information if the {{ .Name }} has been deleted from the database.
func ({{ shortname .Name }} *{{ .Name }}) Deleted() bool {
	return {{ shortname .Name }}._deleted
}

// Insert inserts the {{ .Name }} to the database.
func ({{ shortname .Name }} *{{ .Name }}) Insert(db Queryer) error {
	var err error

	// if already exist, bail
	if {{ shortname .Name }}._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
    const sqlstr = `INSERT INTO {{ .Table.Schema }}.{{ .Table.Name }} (` +
		`{{ colnames .Fields .Table.PrimaryKey }}` +
		`) VALUES (` +
		`{{ colvals .Fields .Table.PrimaryKey }}` +
		`) RETURNING {{ colname .Table.PrimaryKey }}`

	// run query
	XOLog(sqlstr, {{ fieldnames .Fields (shortname .Name) .Table.PrimaryKey }})
	err = db.QueryRow(sqlstr, {{ fieldnames .Fields (shortname .Name) .Table.PrimaryKey }}).Scan(&{{ shortname .Name }}.{{ .Table.PrimaryKey }})
	if err != nil {
		return err
	}

	// set existence
	{{ shortname .Name }}._exists = true

	return nil
}

// Update updates the {{ .Name }} in the database.
func ({{ shortname .Name }} *{{ .Name }}) Update(db Queryer) error {
	var err error

	// if doesn't exist, bail
	if !{{ shortname .Name }}._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if {{ shortname .Name }}._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
    const sqlstr = `UPDATE {{ .Tabl.Schema }}.{{ .Table.Name }} SET (` +
		`{{ colnames .Fields .Table.PrimaryKey }}` +
		`) = ( ` +
		`{{ colvals .Fields .Table.PrimaryKey }}` +
		`) WHERE {{ colname .Table.PrimaryKey }} = ${{ colcount .Fields .Table.PrimaryKey }}`

	// run query
	XOLog(sqlstr, {{ fieldnames .Fields (shortname .Name) .Table.PrimaryKey }}, {{ shortname .Name }}.{{ .Table.PrimaryKey }})
	_, err = db.Exec(sqlstr, {{ fieldnames .Fields (shortname .Name) .Table.PrimaryKey }}, {{ shortname .Name }}.{{ .Table.PrimaryKey }})
	return err
}

// Save saves the {{ .Name }} to the database.
func ({{ shortname .Name }} *{{ .Name }}) Save(db Queryer) error {
	if {{ shortname .Name }}.Exists() {
		return {{ shortname .Name }}.Update(db)
	}

	return {{ shortname .Name }}.Insert(db)
}

// Upsert performs an upsert for {{ .Name }}.
//
// NOTE: PostgreSQL 9.5+ only
func ({{ shortname .Name }} *{{ .Name }}) Upsert(db Queryer) error {
	var err error

	// if already exist, bail
	if {{ shortname .Name }}._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
    const sqlstr = `INSERT INTO {{ .Table.Schema }}.{{ .Table.Name }} (` +
		`{{ colnames .Fields }}` +
		`) VALUES (` +
		`{{ colvals .Fields }}` +
		`) ON CONFLICT ({{ colname .Table.PrimaryKey }}) DO UPDATE SET (` +
		`{{ colnames .Fields }}` +
		`) = (` +
		`{{ colprefixnames .Fields "EXCLUDED" }}` +
		`)`

	// run query
	XOLog(sqlstr, {{ fieldnames .Fields (shortname .Name) }})
	_, err = db.Exec(sqlstr, {{ fieldnames .Fields (shortname .Name) }})
	if err != nil {
		return err
	}

	// set existence
	{{ shortname .Name }}._exists = true

	return nil
}

// Delete deletes the {{ .Name }} from the database.
func ({{ shortname .Name }} *{{ .Name }}) Delete(db Queryer) error {
	var err error

	// if doesn't exist, bail
	if !{{ shortname .Name }}._exists {
		return nil
	}

	// if deleted, bail
	if {{ shortname .Name }}._deleted {
		return nil
	}

	// sql query
    const sqlstr = `DELETE FROM {{ .Table.Schema }}.{{ .Table.Name }} WHERE {{ colname .Table.PrimaryKey }} = $1`

	// run query
	XOLog(sqlstr, {{ shortname .Name }}.{{ .Table.PrimaryKey }})
	_, err = db.Exec(sqlstr, {{ shortname .Name }}.{{ .Table.PrimaryKey }})
	if err != nil {
		return err
	}

	// set deleted
	{{ shortname .Name }}._deleted = true

	return nil
}
